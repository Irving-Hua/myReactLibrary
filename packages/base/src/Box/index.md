# Box

Base layout component built on MUI Box.

```jsx
import { Box } from '@xui/base';

export default () => (
  <Box sx={{ p: 2, bgcolor: 'primary.main', color: 'common.white' }}>
    Hello XUI
  </Box>
);
```
