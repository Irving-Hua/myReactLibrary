/**
 * 第 15 章 — infer 入门
 */

/**
 * Challenge 15.1
 * 实现 `ElementType<T>`：若 T 为 Array<infer U> 则 U，否则 T。
 */
// TODO
export type ElementType<T> = any;

/**
 * Challenge 15.2
 * 实现 `ReturnTypeOf<T>`：若 T 为 (...args: never[]) => infer R 则 R，否则 never。
 */
// TODO
export type ReturnTypeOf<T> = any;
