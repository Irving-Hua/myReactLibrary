/**
 * 第 13 章 — 可辨识联合（Discriminated Union）
 */

/**
 * Challenge 13.1
 * 定义 Shape：Circle { kind: 'circle'; radius: number } 与 Rect { kind: 'rect'; w: number; h: number } 的联合。
 */
// TODO
export type Shape = any;

/**
 * Challenge 13.2
 * 实现 `area(shape: Shape): number`，用 kind 收窄后分别计算面积。
 */
export function challenge_13_2(shape: Shape): number {
  // TODO
  return 0;
}
